PORT=8080
LOG_LEVEL=debug
SESSION_MAX_TRAINEES=10
# Comma-separated list of allowed origins for CORS
# Use '*' for development (allows all origins)
# For production, specify exact origins (e.g., https://sire.example.com,http://localhost:3000)
ALLOWED_ORIGINS=*
