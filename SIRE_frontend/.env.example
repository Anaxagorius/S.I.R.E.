# Base URL for the SIRE backend (no trailing slash).
# Accepts either the bare host (http://localhost:8080)
# or the full API base (http://localhost:8080/api) — both are handled automatically.
VITE_API_BASE=http://localhost:8080
